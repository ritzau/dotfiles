# ~/.zshenv - sourced for ALL shells (login, interactive, scripts)
# Keep this fast: only env vars and PATH setup.

ZDOTDIR_DOTFILES="${${(%):-%x}:A:h}"

for f in "$ZDOTDIR_DOTFILES"/zshenv.d/<0-99>-*.zsh(N); do
  source "$f"
done
